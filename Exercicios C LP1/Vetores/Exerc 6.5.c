#include <stdio.h>

int Str_Len(char* STR){
	char i = 0;
	int soma=0;
		while (STR[i]!='\0'){
			soma +=1;
			i+=1;
	}
	return soma;

}

void strjoin2 (char* somaStr, char* str1, char* str2){
		

}

int main (void){
	int len;
	char str1[100];
	char str2[100];	
	printf("Escreva a string desejada: \n");
	scanf("%s", &str);
	lenT= Str_Len(str1);
	lenT+= Str_Len(str2);
	char somaStr[len];	
    strjoin2(somaStr,str1,str2);
	for
	return 0;

}
